management:
  endpoints:
    web:
      exposure:
        include: "*"

logging:
  level:
    org.springframework: INFO
    ms.safi: DEBUG

spa:
  devserver:
    runner:
      enabled: true
    proxy:
      enabled: true
